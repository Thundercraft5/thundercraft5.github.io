---
s: 0
---
import { giantText, centerText } from "./404.module.scss"

export const metadata = {
  title: 'Understanding AI',
  description: 'A brief overview on neural networks and language models.',
  alternates: {
    canonical: '/n/ai',
    giantText,
    giantText
  }
};

export const k = console.log("k")

<div className={`${giantText} ${ centerText }`}>
  <h1>404</h1> 
  <p>Page not found</p>
</div>